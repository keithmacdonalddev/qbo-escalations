'use strict';

const SCENARIO_DEFINITIONS = Object.freeze({
  disconnected: {
    state: 'disconnected',
    statusLabel: 'Not connected',
    summary: 'Questrade is not connected.',
    action: 'Live access is off, and no token or portfolio data is saved.',
    credentialState: 'not-stored',
  },
  'healthy-margin': {
    state: 'connected',
    statusLabel: 'Connected',
    summary: 'The simulated Margin account is connected and its last saved snapshot is available.',
    action: 'Review the safe account label and simulated status. No financial values are loaded in Stage 1.',
    credentialState: 'stored',
    revocationState: 'not-requested',
    safeAccountId: 'margin-demo-01',
    selectedAccountKey: 'margin-demo-01',
    accounts: [{ accountKey: 'margin-demo-01', label: 'Margin account', accountType: 'Margin', status: 'Active', isPrimary: true }],
    serviceHealth: { accounts: 'available', balances: 'available', positions: 'available', orders: 'available', executions: 'available' },
    lastVerifiedAt: '2026-08-14T18:30:00.000Z',
    lastCheckAt: '2026-08-14T18:30:00.000Z',
    lastSuccessfulSyncAt: '2026-08-14T18:30:00.000Z',
    lastSnapshotId: 'snapshot-demo-001',
    canDisconnect: true,
    canRetryVerification: true,
  },
  'choose-account': {
    state: 'account-selection-required',
    statusLabel: 'Choose account',
    summary: 'Two simulated Margin accounts are available.',
    action: 'Choose one opaque account label. Account numbers are never shown.',
    credentialState: 'stored',
    accounts: [
      { accountKey: 'margin-demo-01', label: 'Margin account 1', accountType: 'Margin', status: 'Active', isPrimary: true },
      { accountKey: 'margin-demo-02', label: 'Margin account 2', accountType: 'Margin', status: 'Active', isPrimary: false },
    ],
    serviceHealth: { accounts: 'available', balances: 'not-checked', positions: 'not-checked', orders: 'not-checked', executions: 'not-checked' },
    canDisconnect: true,
  },
  'partial-access': {
    state: 'partial',
    statusLabel: 'Needs attention',
    summary: 'Some simulated read-only checks completed and others did not.',
    action: 'Retry without replacing the last confirmed evidence.',
    credentialState: 'stored',
    safeAccountId: 'margin-demo-01',
    selectedAccountKey: 'margin-demo-01',
    accounts: [{ accountKey: 'margin-demo-01', label: 'Margin account', accountType: 'Margin', status: 'Active', isPrimary: true }],
    serviceHealth: { accounts: 'available', balances: 'available', positions: 'offline', orders: 'available', executions: 'offline' },
    lastCheckAt: '2026-08-14T18:35:00.000Z',
    lastVerifiedAt: '2026-08-14T18:30:00.000Z',
    previousSnapshotAvailable: true,
    canDisconnect: true,
    canRetryVerification: true,
  },
  offline: {
    state: 'offline',
    statusLabel: 'Questrade unavailable',
    summary: 'The simulated provider could not be reached.',
    action: 'The saved authorization and previously confirmed evidence remain unchanged.',
    credentialState: 'stored',
    safeAccountId: 'margin-demo-01',
    selectedAccountKey: 'margin-demo-01',
    accounts: [{ accountKey: 'margin-demo-01', label: 'Margin account', accountType: 'Margin', status: 'Active', isPrimary: true }],
    serviceHealth: { accounts: 'available', balances: 'offline', positions: 'offline', orders: 'offline', executions: 'offline' },
    lastCheckAt: '2026-08-14T18:40:00.000Z',
    lastVerifiedAt: '2026-08-14T18:30:00.000Z',
    previousSnapshotAvailable: true,
    canDisconnect: true,
    canRetryVerification: true,
  },
  'rate-limited': {
    state: 'rate-limited',
    statusLabel: 'Try again later',
    summary: 'The simulated provider asked the app to wait before checking again.',
    action: 'Wait briefly, then retry without changing the saved authorization.',
    credentialState: 'stored',
    safeAccountId: 'margin-demo-01',
    selectedAccountKey: 'margin-demo-01',
    accounts: [{ accountKey: 'margin-demo-01', label: 'Margin account', accountType: 'Margin', status: 'Active', isPrimary: true }],
    serviceHealth: { accounts: 'available', balances: 'rate-limited', positions: 'rate-limited', orders: 'rate-limited', executions: 'rate-limited' },
    lastCheckAt: '2026-08-14T18:40:00.000Z',
    lastVerifiedAt: '2026-08-14T18:30:00.000Z',
    previousSnapshotAvailable: true,
    canDisconnect: true,
    canRetryVerification: true,
  },
  'token-expired': {
    state: 'reauthorization-required',
    statusLabel: 'Reauthorization required',
    summary: 'The simulated refresh attempt failed, so saved data remains visible but live access is paused.',
    action: 'A future live version will ask you for a new token in Questrade Settings.',
    credentialState: 'renewal-required',
    safeAccountId: 'margin-demo-01',
    lastSuccessfulSyncAt: '2026-08-14T17:45:00.000Z',
    lastSnapshotId: 'snapshot-demo-001',
    previousSnapshotAvailable: true,
    canDisconnect: true,
  },
  'revocation-pending': {
    state: 'revocation-pending',
    statusLabel: 'Revocation pending',
    summary: 'Local access is off, but simulated remote revocation was not confirmed.',
    action: 'Retry revocation or manually revoke in Questrade before removing the local connection.',
    credentialState: 'revocation-pending',
    revocationState: 'pending',
    safeAccountId: 'margin-demo-01',
    selectedAccountKey: 'margin-demo-01',
    accounts: [{ accountKey: 'margin-demo-01', label: 'Margin account', accountType: 'Margin', status: 'Active', isPrimary: true }],
    serviceHealth: { accounts: 'not-checked', balances: 'not-checked', positions: 'not-checked', orders: 'not-checked', executions: 'not-checked' },
    previousSnapshotAvailable: true,
    canDisconnect: true,
    canForgetLocal: true,
  },
  'malicious-api-server': {
    state: 'blocked',
    statusLabel: 'Unsafe server blocked',
    summary: 'The simulated Questrade response named an unapproved server. The app refused to send credentials.',
    action: 'No action is needed. This is the expected security response.',
    credentialState: 'not-stored',
    securityCode: 'QUESTRADE_API_HOST_BLOCKED',
  },
  locked: {
    state: 'locked',
    statusLabel: 'Credential locked',
    summary: 'The simulated Windows credential key cannot be unlocked by the current Windows user.',
    action: 'A future live setup will provide a repair action without exposing the token.',
    credentialState: 'locked',
  },
  'key-store-unavailable': {
    state: 'unavailable',
    statusLabel: 'Credential protection unavailable',
    summary: 'The simulated computer cannot provide a supported local credential key store.',
    action: 'Live connection stays disabled until credential encryption is available.',
    credentialState: 'unavailable',
  },
  'service-unavailable': {
    state: 'degraded',
    statusLabel: 'Questrade unavailable',
    summary: 'The simulated service is unavailable. The last complete saved snapshot remains available.',
    action: 'Wait and retry later; the app will not replace good saved data with a partial result.',
    credentialState: 'stored',
    safeAccountId: 'margin-demo-01',
    lastSuccessfulSyncAt: '2026-08-14T17:45:00.000Z',
    lastSnapshotId: 'snapshot-demo-001',
    previousSnapshotAvailable: true,
    canDisconnect: true,
    canRetryVerification: true,
  },
});

function isQuestradeFixtureModeEnabled(env = process.env) {
  return env.NODE_ENV !== 'production' && env.QUESTRADE_DEV_FIXTURES !== '0';
}

function listQuestradeScenarios() {
  return Object.entries(SCENARIO_DEFINITIONS).map(([id, fixture]) => ({ id, label: fixture.statusLabel }));
}

function buildQuestradeFixtureStatus(scenario = 'disconnected') {
  const selected = SCENARIO_DEFINITIONS[scenario] ? scenario : 'disconnected';
  return {
    ok: true,
    provider: 'questrade',
    mode: 'simulated',
    liveAccessEnabled: false,
    readOnly: true,
    accountType: 'Margin',
    secureStorageReady: selected !== 'key-store-unavailable',
    scenario: selected,
    ...SCENARIO_DEFINITIONS[selected],
  };
}

module.exports = {
  SCENARIO_DEFINITIONS,
  buildQuestradeFixtureStatus,
  isQuestradeFixtureModeEnabled,
  listQuestradeScenarios,
};
