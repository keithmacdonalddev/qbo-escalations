PROMPT_VERSION: P34

Read one screenshot of the QBO escalation form.

Output the completed canonical form only, in this exact order:

COID/MID:
CASE:
CLIENT/CONTACT:
CX IS ATTEMPTING TO:
EXPECTED OUTCOME:
ACTUAL OUTCOME:
KB/TOOLS USED:
TRIED TEST ACCOUNT:
TS STEPS:

Literal transcription is the priority.
Copy the characters visible in the screenshot, not the cleaned-up sentence you expect.
The screenshot wins over spelling, grammar, business meaning, sentence position, and likely intent.
Do not proofread, spell-check, grammar-fix, title-case, normalize, summarize, or infer.
If visible text looks wrong, keep it wrong.
Preserve visible letter order, capitalization, digits, punctuation, accents, and spacing.

Extraction rules:
- First locate the nine canonical labels and their value regions.
- A value region starts after that field's visible separator and ends before the next canonical label or row begins.
- A separator may look like a colon, semicolon, quote-like mark, backtick-like mark, distorted colon, or other small punctuation immediately after a label.
- Use screenshot labels only to find the matching value. Always output the canonical labels exactly as listed above.
- If a screenshot label is shortened, misspelled, distorted, or says `CLIENT` instead of `CLIENT/CONTACT`, map it to the closest canonical label.
- Copy each value only into its own matching output line.
- Do not move text upward, downward, or sideways to fill another field.
- For side-by-side or stacked rows, a value belongs to the label in the same row/block.
- If the next canonical label appears before any value for the current field, leave the current field blank.
- If a visible label has no value after it, leave that output line blank.
- Write `N/A` only when the characters `N/A` are visible in that same field's value region.
- If `N/A` appears beside TEST ACCOUNT, put `N/A` on TRIED TEST ACCOUNT only.
- For KB/TOOLS USED, leave it blank unless a value is visible after KB/TOOLS USED.

EXPECTED OUTCOME and ACTUAL OUTCOME are separate fields.
Check both fields together before final output because one may be blank.
If EXPECTED OUTCOME has no value and ACTUAL OUTCOME has a value, leave EXPECTED OUTCOME blank and put the value on ACTUAL OUTCOME.
If EXPECTED OUTCOME has a value and ACTUAL OUTCOME has no value, put the value on EXPECTED OUTCOME and leave ACTUAL OUTCOME blank.
Never move a value between these two fields to make the form look more complete.

Character fidelity rules:
- For names and words, copy every visible letter exactly.
- Names are not dictionary words. Do not replace a visible name with a more common-looking name.
- If the first character of a name is uncertain, choose only the character visibly inside the value region after the separator.
- Do not borrow letters from the label, separator, row border, nearby fields, or memory.
- Preserve visible misspellings and transposed letters. If the image shows `Termianted`, output `Termianted`; if it shows `Terminated`, output `Terminated`.
- Preserve visible case. If the image shows `paycheques`, output `paycheques`; if it shows `Paycheques`, output `Paycheques`.
- If a word appears lowercase after a label or in the middle of a sentence, keep it lowercase unless its first letter is visibly uppercase.
- Preserve accented letters exactly as visible. Do not convert `Dépôt` to `Depot`.
- For COID/MID and CASE, copy every visible digit from left to right.
- For long numeric IDs, verify the digit sequence and digit count twice before final output.
- Keep repeated adjacent digits only when each repeated digit is visibly present.
- Do not shorten, compress, or normalize identifiers.

Punctuation and spacing rules:
- Preserve visible apostrophes and quote marks as characters, not decoration.
- Straight ASCII apostrophe `'` and curly apostrophe `’` are different characters.
- Straight ASCII quote `"` and curly quotes `“` or `”` are different characters.
- Output curly apostrophes or curly quotes only when the glyph is visibly curved/directional in the screenshot.
- If an apostrophe or quote mark is small, blurry, or ambiguous, use the straight ASCII character: `'` or `"`.
- Do not turn `EE's` into `EE’s`, `can't` into `can’t`, or `"word"` into `“word”` unless those curly characters are visibly present.
- Visible `can't` or `can’t` must not become `cant`.
- Do not add apostrophes, quotes, commas, periods, or spaces unless they are visible.
- Do not insert commas inside values unless the comma is visibly present.
- After the canonical label colon, copy the value spacing from the screenshot.
- If the value begins immediately after the screenshot separator, begin it immediately after the canonical colon.
- If the value begins after one visible space after the screenshot separator, keep one space after the canonical colon.
- Preserve visible punctuation spacing inside values.
- If a quote mark touches a word in the image, keep it touching the word.
- If a long value wraps visually across multiple screenshot lines, keep it in the same output field and join the wrapped text with the visible word spacing.

Before final output, silently verify:
CLIENT/CONTACT letters.
EXPECTED OUTCOME vs ACTUAL OUTCOME placement.
Blank fields and `N/A` placement.
Digit sequences.
Misspellings and transposed letters.
Lowercase vs uppercase letters.
Apostrophe/quote character type.
Spacing after each canonical colon.

No prose. No markdown. No JSON. No duplicate form. No extra fields.
