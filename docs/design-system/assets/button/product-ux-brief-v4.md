# Button Documentation Cover — PDF Page-One Translation

## User goal

Open the Button documentation and encounter the same clear, composed first-page experience as the supplied PDF: component identity, one exact summary, and a substantial framed visual explanation.

## Entry and next action

The route opens with `Component design specification`, `Button`, the PDF's concise definition, and a large light showcase. The reader sees the canonical specimen first and then scrolls into the unchanged visual overview.

## Information architecture

The existing identity → visual family → detailed reference sequence remains. The identity stage now follows the actual PDF page-one composition rather than an abstract interpretation of minimalism.

## Stable frame

The global documentation shell, route, later chapters, and production Button remain unchanged. The cover owns only its heading, definition, and framed summary.

## Initially visible

- `Component design specification`
- `Button`
- `A generic, accessible action primitive with restrained semantic intent, predictable states, and a scalable size system.`
- one large light showcase
- one full-width live production Button labelled `Button`
- three compact summaries: Intent, State, and Size

## Progressive disclosure

The light showcase provides only the three concepts present on the PDF cover. Detailed distinctions—including the production separation of priority and destructive tone—remain in the later specification map and API chapters.

## State matrix

The page-one specimen is resting and interactive. Loading, disabled, failure, recovery, confirmation, completion, and reduced-motion evidence remain in the unchanged later chapters.

## Continuity

Normal document scrolling, anchors, route behavior, DOM order, and focus behavior remain unchanged.

## Wording

Use the PDF's exact cover definition and generic `Button` specimen label. Summary wording follows the PDF: Intent, State, and Size.

## Trust and privacy

The cover uses no business data, performs no external action, and persists nothing.

## Desktop and mobile

Desktop follows the PDF's left-aligned heading and wide light showcase with a nearly full-width Button, two summary pills on the first row, and Size centered beneath them. At exactly 390px, the heading remains left aligned, the showcase stays fully visible, and the three summaries stack without horizontal page overflow.

## Keyboard and accessibility

The production Button retains native semantics and visible focus. The summary is a named list with three list items. The light surface must preserve readable text and an unclipped focus ring.

## Reduced motion

No new motion is introduced.

## Production versus development

The summary communicates the component's design model, not source paths, test controls, status badges, or release machinery.

## Visual craft and Slate fit

Translate the PDF's composition into Slate: dark application background, blue accent, crisp white type, and one light rounded showcase. Preserve the PDF's proportion and visual order without copying document-only footer metadata. Do not substitute a dark empty placeholder or remove the showcase.

## Scope and approval triggers

Allowed: Button cover markup and CSS, focused docs assertion, sanitized v4 evidence, release record, and correction of the overly broad skill rule. Changing Button behavior, later chapters, shared docs chrome, other component pages, dependencies, or runtime control requires renewed approval.
