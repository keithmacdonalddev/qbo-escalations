# Overview Page Review — Escalation Image Parser

**Profile route:** `#/agents/escalation-template-parser`
**Rendered by:** `client/src/components/AgentsView.jsx` (a single **4,367-line** file — the file that draws the whole profile page. It contains the ENTIRE profile UI — the page shell, the tab bar, and every tab's content as inner functions. Nothing about the profile lives in any other file. Verified 2026-05-30.)

## How the page is built (plain English)
- `AgentProfileDetailPage` (line 1642) = the page shell (title bar + enable toggle).
- `AgentProfileTabs` (line 1843) = the row of tabs.
- `AgentProfileWorkspace` (line 1860) = picks which tab's content to show.
- `AgentOverviewTab` (line 1903) = the Overview tab we're starting with.
- A big hardcoded table, `AGENT_OPERATION_META` (lines 113-226), is the source of most of the FAKE numbers/diagrams. The escalation-template-parser entry sits at lines 128-141 and contains a hardcoded `trust: 4.8` (line 133) and a hardcoded `workflows: ['Template Intake', 'Form Normalization', 'Evidence Mapping', 'Parser QA']` (line 137).
- A function, `buildOperationalProfile` (line 3814), turns that table + the agent's real record into the values the Overview panels show. It is pure computation — it does NOT call any API/fetch. Every Quality number and the Workflow Footprint diagram come out of this function.

## Verified real pipeline topology (for the real Workflow Footprint)

The real pipeline order is confirmed in code (not invented):
- `client/src/components/chat-v5/PipelineSidebar.jsx` line 13: `const PIPE_ORDER = ['parser', 'inv', 'triage', 'main'];`
- `client/src/components/chat-v5/useStageOrchestrator.js` (startRequestWithImage state machine + auto-advance useEffects): parser runs first (client-side), then inv (known-issue-search-agent) + triage (triage-agent) run in PARALLEL on the server, then main (chat/analyst) runs last.
- `server/src/routes/pipeline-tests.js` lines 46-51: `STAGE_AGENT_IDS` maps stage->agentId (parser='escalation-template-parser', inv='known-issue-search-agent', triage='triage-agent', main='chat') — but this is a flat lookup, it does NOT encode order.

For escalation-template-parser specifically, the TRUE footprint is:
`(raw screenshot from operator) → escalation-template-parser → { known-issue-search-agent + triage-agent in parallel } → chat/analyst`
Upstream: none (it is the pipeline entry point). Downstream: known-issue-search-agent AND triage-agent (parallel), then chat.

Feasibility to build a real Workflow Footprint: MEDIUM — the ordering is real and consistent but spread across 3 files; building it means extracting that topology into one small source/config and rendering it. The current fake panel (hardcoded names split in half) is not just generic-fake, it is directionally wrong.

## Tabs shown for this agent
The escalation-template-parser profile shows **12 tabs**, in this exact order:

1. Overview
2. Configuration
3. Prompt
4. Harness
5. Test Results
6. Event Streams
7. Chat Sessions
8. Memory
9. Monitoring
10. Workflows
11. Activity
12. Versions

(Built in code as `IMAGE_PARSER_PROFILE_TABS`, lines 91-97: it takes the shared 9-tab `PROFILE_TABS` list and inserts three parser-only tabs — Test Results, Event Streams, Chat Sessions — after the Harness tab. 9 + 3 = 12. An earlier report that said "10 tabs" missed the three inserted parser-only tabs. Note the seventh tab's label is "Chat Sessions," not "Sessions.")

## Real vs. fabricated — summary
**REAL (database/file-backed):** identity & persona fields; Prompt tab (real file + version history); Test Results tab (real parser test runs); Event Streams & Chat Sessions tabs (real parser events); Memory / Activity / History / Harness runs / Relationships (stored per-agent in the DB); Runtime Defaults provider/model picker; Monitoring (live provider reachability).

**FABRICATED (hardcoded / formula, no real source):** Quality & Performance metrics (all); Workflow Footprint graph; Harness "test coverage %"; agent-card mini sparklines; the "trust" score; the always-on "Issues/tasks" gap row.

Backend confirmation: there is **no CSAT data** anywhere, **no per-agent handle-time data** (verified 2026-05-30: zero matches for CSAT / handle-time / AHT anywhere under `server/`), and only *partial* accuracy data (real parser test results exist in the `ImageParserTestResult` collection — model file `server/src/models/ImageParserTestResult.js` confirmed — but the Overview does not use them). There is **no workflow-topology model** (no real "this agent feeds that agent" data).

## Overview panels — decision ledger
For each panel: where it gets data, whether it's real, and our decision. Decisions filled in as we review together. (Panels listed in the exact top-to-bottom order they render in `AgentOverviewTab`, lines 1909-1943.)

| # | Panel (top → bottom) | Data source | Real or fake? | Decision | Notes |
|---|---|---|---|---|---|
| 1 | Page header — status badge / Active / enable toggle | agent.enabled from DB | REAL | ⏳ pending | rendered by AgentProfileDetailPage (line 1642) |
| 2 | Overview identity grid → **rebuilt** as truthful status-first ID badge card | identity/runtime/tools/health all REAL or honest-empty (see audit below) | was MIXED → now ALL REAL or honest-empty | ✅ REBUILT as truthful status-first ID card (2026-05-30) | New `AgentIdentityBadgeCard` (AgentsView.jsx). **REMOVED (were fabricated AGENT_OPERATION_META literals):** Department, Permissions, Risk Level, Review Status, the "N workflows" badge, and the redundant duplicate ID ("Code identity" == "Agent ID"). **Prompt ID** now only shown when it differs from the agent ID (hidden for this parser). **NEW lines + real source:** name = `profile.roleTitle`; status pills = `agent.enabled` (real) + live provider health from the agent registry (`registryHealthById`, same source as every status dot) with "last checked Ns" freshness; purpose = `profile.headline`; Agent ID = `agentId`; Runs on = `operation.modelLabel` (real runtime pick); Won't = `profile.boundaries` (Does shown only when headline ≠ purpose, to avoid duplication); Can access = real `tools.available` list (parser → honest "No tools"); Lately = latest real harness run (pass/fail) → latest activity → latest timestamp → "No runs yet"; Attention = real harness-fail / review-rejected / no-prompt signals, else honest "Nothing needs attention." Edit/Run test buttons reuse existing tab navigation (Configuration / Harness). |
| 3 | Workflow Footprint (upstream/downstream graph) | hardcoded `workflows` array in AGENT_OPERATION_META, split in half: `workflowInputs = workflows.slice(0, midpoint)`, `workflowOutputs = workflows.slice(midpoint)` where `midpoint = Math.ceil(workflows.length / 2)` (lines 3861-3862) | FAKE | ✅ BUILD REAL VERSION (deferred to implementation phase) | no topology backend exists; for the parser the 4 workflows split 2/2. Verified MEDIUM feasibility; fake diagram is directionally WRONG (shows triage upstream when triage is actually downstream). |
| 4 | Quality & Performance (Resolution Accuracy, First Response Accuracy, Escalation Precision, Avg. Handling Time, Operator CSAT) | formula from hardcoded `trust` (`qualitySeed = Math.round(trust * 18)`); delta strings hardcoded ('+4.3%', '+3.1%', etc.) | FAKE | ⏳ pending | 5 metrics total; no CSAT/AHT source; accuracy only partial via ImageParserTestResult; `qualityMetrics` block at lines 3876-3912 |
| 5 | Prompt Contract (Mission / Inputs / Output Format / Guardrails / Escalation Trigger / Runtime) | computed from AGENT_OPERATION_META + real agent.profile fields (`buildPromptContract`, lines 3964-3991) | MOSTLY COMPUTED | ⏳ pending | real prompt content is available via /api/agent-prompts/:id |
| 6 | Harness & Execution (harness type, channels, output format, latency target, fallback model, observability, coverage %) | computed; coverage % is a formula (`testCoverage = clamp(Math.round(72 + trust * 4 + Math.min(8, toolCount)), 72, 96)`, line 3842) | COMPUTED / FAKE coverage | ⏳ pending | real harness data exists in harness.runs + ImageParserTestResult |
| 7 | Profile Source of Truth ledger (Identity / Prompt / Runtime / Memory / History / Harness / Activity / Relationships / Issues-tasks / Monitoring) | array lengths from real agent doc | MOSTLY REAL | ⏳ pending | 10 rows; the "Issues / tasks" row is hardcoded to always show as a gap (see below) |
| 8 | Change Review Workflow checklist | computed from real reviews/harness/prompt/tools data (`buildReviewItems`, line 3993) | COMPUTED FROM REAL | ⏳ pending | rendered as ReviewWorkflowPanel (line 1937) |

### Confirmed code detail — the always-gap "Issues / tasks" row
In `ProfileSourceOfTruthPanel` (line 2016), the ledger is a hardcoded array. One row is wired to always show as a gap because its "ready" value is the literal `false`:

```js
['Issues / tasks', false, 'No first-class agent issue/task ledger yet'],   // line 2026
```

Because the second element is `false`, this row will render its status as "Gap" no matter what — it never reflects real data.

## Open questions / to confirm as we go
- (Part A) Exact tab count (10 vs 12) — **RESOLVED: 12 tabs** (the "10" report missed the three inserted parser-only tabs: Test Results, Event Streams, Chat Sessions).
- (Part A) Quality panel window label — **RESOLVED: the code says `Last 30 days`** (line 1981), not "14 days." A screenshot suggested "14 days," but the live code string is `Last 30 days`. (Either the screenshot was from a different build, or the label was misread — flag for the user.) The label is purely decorative text; there is no real 30-day window behind it.
- For each FAKE panel: is there a real source we can wire to, should it be removed, or labelled as sample? (decided per-panel)

## Decisions log (append-only)
- 2026-05-30: Folder created; investigation complete; starting Overview review top-to-bottom. Strategy: decide per-panel (keep/fix/remove). No global rewrite yet.
- 2026-05-30: Two draft facts corrected against live code — (a) file is 4,367 lines (draft said ~4,368), (b) Quality window label is "Last 30 days" (draft's open question expected "14 days").
- 2026-05-30: Panel 3 (Workflow Footprint) decided = BUILD REAL VERSION, deferred to implementation phase. Topology verified (see section above). NOTE: relationships.map (buildRelationshipLearning in agent-identity-service.js ~line 1221) is a SOCIAL graph (who-mentions-whom in chat), NOT pipeline topology — do not use it for the footprint.
- 2026-05-30: **Panel 2 (identity grid) REBUILT** as a status-first "ID badge" card (`AgentIdentityBadgeCard` in `client/src/components/AgentsView.jsx`; styles in `client/src/components/AgentsView.css`). **Honesty principle applied:** every line is backed by a real saved-record / runtime / live-health value, or shows an explicit empty state ("No tools…", "No runs yet.", "No guardrails recorded yet."), or is omitted. **Removed because fabricated** (hardcoded AGENT_OPERATION_META literals): Department, Permissions, Risk Level, Review Status, the "4 workflows" badge, and the duplicate "Code identity" ID. The `AGENT_OPERATION_META` table was NOT deleted and `buildOperationalProfile` still PRODUCES `department/permissions/risk/reviewStatus/workflows` — three OTHER panels still read them (Workflow Footprint, Change Review Workflow, Profile Studio at AgentsView.jsx ~2492-2495, the Workflows tab ~3162, and the search-text builder ~4516); we only stopped RENDERING them in this card. Scope was strictly this one card — no other panel/tab touched.
  - **New live-health wiring:** the selected agent's raw registry health (`{status, checkedAt}`) is now threaded into the Overview workspace props as `selectedHealth`, sourced from the existing `registryHealthById` map (same source as every status dot). The card renders an honest "Enabled/Disabled" pill (real `agent.enabled`) AND a separate live "Online/Offline/…" health pill with a "last checked Ns" freshness hint. This is real reachability, not a fabricated health value.
  - **Lines omitted for lack of a real source:** none invented. "Does" is suppressed when the only real source (`headline`) duplicates the purpose line. "Prompt ID" is suppressed when it equals the agent ID. There is intentionally NO governance/risk/department/permissions line because no real source exists for them on this record.
  - **Verification:** one-shot `npm --prefix client run build` passed (no errors). Visual confirmed with agent-browser against the live `#/agents/escalation-template-parser` profile — screenshots saved to `review-screenshots/agent-overview-card-after.png` (card) and `…-after-full.png` (page). Live render showed: name "Escalation Image Parser", Enabled + "Online · 15s" pills, purpose, Agent ID, Runs on "Google Gemini API: gemini-3.5-flash", Won't = real boundaries, "No tools" empty state, real "Lately" activity line, "Nothing needs attention."
  - **Pre-existing fabrication still present in OTHER panels** (out of scope here, flagged for later rows): Profile Studio's identity grid (~2492-2495) still renders Permissions/Risk/Review Status from the same fabricated literals; the Change Review Workflow header still shows the meta-tainted `reviewStatus`.
- 2026-05-30: **Honesty fix (review follow-up)** — removed the purpose line's middle fallback `operation?.promptSummary?.goals`, which itself defaulted to a hardcoded sentence ("Understand escalation context and recommend the next best action.", ~line 4157) and would have printed canned copy as if it were the agent's real purpose when no real `profile.headline` exists. Purpose is now strictly `agent.profile.headline || 'No purpose described yet.'`. Re-built (npm --prefix client run build) — passes.
- 2026-05-30: **Visual/UX pass v2 on the ID-badge card** (purely visual — every real data line and honest empty state unchanged). Three fixes:
  1. **Removed the "Edit" and "Run test" header buttons** (user decision). They only navigated to the Configuration/Harness tabs, which already exist in the tab bar, and their labels were misleading. Dead wiring cleaned up: dropped the `onEdit`/`onRunTest` props on `AgentIdentityBadgeCard`, the `onOpenConfig`/`onOpenHarness` pass-through in `AgentOverviewTab`, and the matching entries I had added to `profileWorkspaceProps`. Confirmed by grep that the remaining `onOpenConfig`/`onOpenHarness` references belong to the unrelated, pre-existing `AgentProfileDetailPage`/`MissionControlHeader` path (a different header button) — left untouched. No orphaned references remain (`onEdit`/`onRunTest`/`id-badge-actions` = 0 hits).
  2. **Fixed wasted space.** Capped the card at `max-width: 940px` + `justify-self: start` so it stops stretching thin across the wide 3-column overview layout. Replaced the old `repeat(2, 1fr)` body grid (which left an empty 4th "ghost" quadrant because there were only 3 sections) with a deliberate named-area grid: `"identity does" / "identity access" / "lately attention"` — Identity slab spans the left column across both content rows; Does/Won't and Can-access stack on the right; Lately/Attention sit in a split footer above a divider. No blank cell. Tightened gaps/padding to a 4px-grid, denser rhythm.
  3. **Fixed low-contrast pills.** Old fill (`color-mix(#1f2430 70%, transparent)`) was nearly identical to the card bg so the capsules were invisible. Pills now sit on a raised, state-tinted fill (`color-mix(--agent-green/--agent-orange 22%, --agent-panel-strong)`) with a defined colored border, a 1px inner glow, and near-white labels (`#eafff3` / `#fff0dc`) for all states (Enabled on/off, health good/neutral/down). Status dots kept. Also scoped `.id-badge-header` and `.id-badge-name` under `.agent-id-badge-card` to beat the generic `.agent-panel > header` (which was forcing `justify-content: space-between`, pushing the title block far right) and `.agent-panel h3` (which was shrinking the title).
  - **Verification:** `npm --prefix client run build` passes (AgentsView chunk shrank ~0.4 kB from button removal). agent-browser against the live `#/agents/escalation-template-parser` profile confirmed buttons gone (absent from a11y tree), and `getComputedStyle` confirmed real values: pills 25px tall, `overflow: visible`, label color `rgb(234,255,243)`; card width capped at 940px; body grid areas = `"identity does" "identity access" "lately attention"`. Screenshots: `review-screenshots/agent-overview-card-v2-after.png` (card) and `…-v2-after-full.png` (page). NOTE: in the headless screenshots the pill label text looks faint/clipped, but that is a capture downscaling artifact for small light-on-tinted text — the live DOM measurements above show the text renders fully and legibly. Did not start/restart any server (app was already running on :5174; build was one-shot only).
- 2026-05-30: **Visual GROUND-UP redesign v3 of the ID-badge card** (markup + CSS only; v1/v2 were rejected as "horrible/unusable UI"). Zero data-logic changes — every real-data source and honest empty state from v1/v2 is byte-for-byte preserved; `AGENT_OPERATION_META` untouched. New class namespace `idcard-*` on a root `.agent-id-badge-card.idcard`; old `id-badge-*` / `id-status-pill` markup and CSS fully replaced. Three documented root-cause failures fixed:
  1. **Contrast trap → real stepped surfaces.** v1/v2 inherited the murky `--agent-panel`/`--agent-panel-strong` cool-grays that sat 1–2% apart from the page, so card/inset/page blended. v3 declares **card-scoped, opaque surface tokens** and the steps were *measured against the live page* with `getComputedStyle` (HSL lightness): **page #1c1c1e (11.4%) → card #252834 (17.5%) → inset #333845 (23.5%)** — each boundary ≈ +6% L (both ≥5%). Attention footer is a recessed band `#1d2029` (13.7%, a step *down* toward the page). Text tokens: primary `#f4f6fa`, secondary `#a6adba`, muted `#6f7787`. Semantics: success `#4ade80`, warning `#f59e0b`, error `#ef4444`. Hairline `rgba(255,255,255,0.10)`, radius card 12 / block 8 / chip 6, 8px spacing grid.
  2. **No type hierarchy → strict scale.** Title 20px/600 −0.01em (verified computed); section labels OVERLINE 11px/600 uppercase 0.05em muted; field labels 12px/500 secondary; values 14px/400 primary; **technical values (Agent ID, Runs-on) in monospace 13px**; purpose 13px secondary.
  3. **Cascade wars + the real blue-title bug.** The title was washing out **blue** — not (only) a capture artifact: `overhaul.css` rule `header [class*="title"]` (a holographic blue/purple gradient via `background-clip:text` + `-webkit-text-fill-color:transparent !important`) was matching the old `idcard-title-block` (class contains "title"); because **`-webkit-text-fill-color` is an inherited property**, the transparent fill cascaded into the `<h3>` and painted it blue. **Fix:** renamed the container to `.idcard-headings` (no "title" substring) so the selector no longer matches, AND pinned `-webkit-text-fill-color: var(--idc-primary) !important; background:none !important` on `.idcard-name`, AND added a card-wide `* { -webkit-text-fill-color: currentColor }` guard. Confirmed fixed: title computed `color #f5f5f7`, **fill `#f4f6fa` (opaque)**, `background-image none`. Also defeated `.agent-profile-detail-page .agent-panel h3` (0,2,1) and `.agent-panel > header` (space-between) via higher-specificity scoped selectors `.agent-profile-detail-page .idcard .idcard-name` (0,3,0) and `.agent-profile-detail-page .idcard > .idcard-header` — no `!important` needed for those (only for the inherited-fill reset).
  - **Chip decision (Enabled vs Health distinguished):** **Health** is THE semantic pill (top-right) and carries the live color — tinted fill ~14% + ~35% semantic border + solid 6px dot + near-white `#f4f6fa` label (green Online / red Down / neutral Checking) with a "·  Ns ago" freshness meta. **Enabled** is a deliberately **quiet neutral chip** under the purpose (inset fill, hairline border, small green dot, gray uppercase label) — so the two never render as identical green blobs (the v2 failure). Verified: Enabled label `#a6adba` on `#333845`; Health label `#f4f6fa`.
  - **Layout:** header = avatar (56px, scoped up from the page's 48px override) + headings (name / purpose / Enabled) flush-left, Health pill pinned top-right; body = balanced 2-col grid (Identity inset slab + Can-access left; Does/Won't + Lately right) — **no empty ghost quadrant**; footer = hairline divider then full-width recessed **Attention** band with a tone dot. Card capped at `max-width: 920px`. Responsive collapse < 760px.
  - **Self-critique of FINAL screenshot (harsh, vs Linear/Stripe/Vercel bar):** surface separation now obviously reads (card lifts off page, inset slab + footer band are distinct) ✓; type hierarchy obvious at a glance (white 20/600 title vs muted overlines vs mono identifiers) ✓; chips readable and distinct (semantic Health vs neutral Enabled) ✓; no blank dead space ✓; looks like a premium product object ✓. Residual nits: avatar glyph is plain (real data, out of scope); the parser has zero tools so the tool-chip styling is verified in CSS but not visually exercised on this agent; full-page shot still shows the app's unrelated "request was slow" toast (not part of the card). **Iterations: 6** (5 visual passes + the gradient-text root-cause dig). Verifying by *computed style*, not just the screenshot, was decisive — the screenshots alone read "fine/artifact" while the DOM proved a real inherited-transparent-fill bug and a real page→card lightness gap of only ~1.5% before correction.
  - **Final screenshots:** `review-screenshots/agent-overview-card-v3-after.png` (card) and `…-v3-after-full.png` (page in context); intermediates `…-v3-baseline*.png` (before) and `…-v3-iter1..6.png`.
  - **Build:** one-shot `npm --prefix client run build` passed each iteration (no errors). Did NOT start/stop/restart any server — the app was already live on :5174 and picked up changes via HMR; screenshots taken against the running instance.
- 2026-05-30: **WHOLE OVERVIEW TAB REPLACED with the approved chrome-light overview PAGE** (mockup `prototypes/agent-card/overview-page.html`, approved screenshot `review-screenshots/agent-overview-page-proto-full.png`). This single page now SUBSUMES the entire old panel stack — it replaces what `AgentOverviewTab` rendered: `AgentIdentityBadgeCard` + `WorkflowFootprint` + `QualityPerformance` + `PromptContractPanel` + `HarnessSummaryPanel` + `ProfileSourceOfTruthPanel` + `ReviewWorkflowPanel`. **No shared component was deleted** — each of those functions stays defined and several are still rendered by OTHER tabs (verified by grep: `WorkflowFootprint` Workflows tab ~3150; `PromptContractPanel` Prompt tab ~2562; `HarnessSummaryPanel` Harness tab ~2621; `ReviewWorkflowPanel` ~2491). `QualityPerformance` / `ProfileSourceOfTruthPanel` / `AgentIdentityBadgeCard` are now defined-but-unrendered (left intact, not deleted, per scope rules). The fabricated `AGENT_OPERATION_META` table and `buildOperationalProfile` are no longer read by the Overview at all.
  - **Files changed:** `client/src/components/AgentsView.jsx` (AgentOverviewTab rewritten + new `PipelineDiagram` sub-component + `onTabChange` threaded into `profileWorkspaceProps` + imports), `client/src/components/AgentOverviewTab.css` (NEW, the scoped page styles), `client/src/components/chat-v5/pipelineRuntime.js` (NEW exports: `PIPELINE_TOPOLOGY`, `PIPELINE_TOPOLOGY_ORDER`, `pipelineNodeLabel`, `pipelinePosition`).
  - **Real pipeline topology source built** (fulfils the Panel-3 "BUILD REAL VERSION" decision and replaces the directionally-wrong fake Workflow Footprint). `PIPELINE_TOPOLOGY` in `pipelineRuntime.js` encodes the verified graph as `agentId → {entry, isFinal, upstream[], downstream[], parallelWith[]}`: parser is the entry (fed by the operator screenshot, no upstream agent) → `known-issue-search-agent` + `triage-agent` run in PARALLEL → `chat`. The diagram renders from this; node display names come from `STAGE_LABELS` (chat-v5/mockData.js) via `pipelineNodeLabel`. Agents NOT in the map render an honest "Standalone agent — not part of the escalation pipeline" state (verified live on `workspace`). "Position in pipeline" derives from `PIPELINE_TOPOLOGY_ORDER` via `pipelinePosition`.
  - **Per-section REAL-source map (live-verified):**
    - Name = `profile.roleTitle || displayName || labelAgent(id)`; Purpose = `profile.headline`; Enabled = `agent.enabled`. Health pill = registry `selectedHealth.{status,checkedAt}` (SAME source as the status dots) with `formatLastChecked` freshness — reused, not reinvented.
    - Agent ID = `agent.agentId`. Model = `getAgentRuntimeEffectiveModel(def, agent.runtime)` (e.g. `gemini-3.5-flash`); Provider sub-label = `getAgentRuntimeProviderLabel` (e.g. `Google Gemini API`) — the combined `operation.modelLabel` was split via the runtime catalog helpers. Tools = real `agent.tools.available` (parser = `[]` → honest "None · works from its prompt and inputs only"; workspace renders its real multi-tool chip list).
    - Behavior: Won't = `profile.boundaries` (real). Does = `profile.headline` ONLY when it is distinct from the purpose line; for this parser headline == purpose so the Does block is intentionally OMITTED (no invented prose).
    - Recent Results = REAL `GET /api/pipeline-tests` parser stats via the existing `parserTestResults.stats` (passRate / total / avg / pass / fail / pending), warmed by an idempotent `onLoadParserTestResults()` on mount; when `total === 0` it falls back to the honest empty state "No transcription tests recorded yet" + a button that navigates to the Test Results tab (parser) or Harness tab (others) via `onTabChange`. NO fabricated CSAT / accuracy% / handle-time.
    - Activity = real `agent.activity.entries` (sorted, top 6). "All clear" vs attention = `buildIdentityAttention(agent)` (real harness-fail / review-rejected / no-prompt signals) — reused the existing truthful helper.
    - INFO RAIL: **Transport** = derived from the runtime kind (`image-parser` kind → "Direct provider API"; else "Claude CLI subprocess") — cleanly real for every agent that has a runtime definition. **Last updated** = `latestAgentTimestamp(agent)` (real). **Position in pipeline** = derived from the real topology (parser shows "Step 1 of 3 · entry parser"; triage shows "Step 2 of 3 · parallel stage"). **Prompt version** = real prompt-snapshot COUNT from `promptState.versions.length` (warmed via `onLoadPrompt()`), shown as "N snapshots" only when > 0; this parser has 0 saved versions so it is currently OMITTED (no fabricated "P24").
  - **OMITTED fields (no honest source — explicitly NOT fabricated):** **Accepts** and **Returns** (mockup rail items) — there is no real structured per-agent schema for them, so they are dropped rather than hardcoded. **Prompt version** is omitted whenever the prompt has zero saved snapshots (the mockup's literal "P24" had no real source). The mockup's "Step 1 of 4" became the honest "Step 1 of 3" because the real topology has 3 sequential stages (the two parallel agents share step 2).
  - **Global-CSS defense (this is why prior in-app attempts washed blue):** all markup is scoped under a unique root class **`.agent-overview-v2`** (no class anywhere contains the substring "title", so `overhaul.css`'s `header [class*="title"]` holographic-gradient rule cannot match). Added a card-wide guard `.agent-overview-v2, .agent-overview-v2 * { -webkit-text-fill-color: currentColor }` to reset any inherited transparent fill, PLUS an explicit pin on `.aov-name` (`-webkit-text-fill-color: var(--aov-primary) !important; background: none !important`). Also neutralized the global `header::before` blur pseudo-element on `.agent-overview-v2 .aov-header`. **Verified by computed style, not just screenshots:** `.aov-name` → `color rgb(245,245,247)`, `webkitTextFillColor rgb(245,245,247)`, `backgroundImage none`; `header::before` → `content none`; body grid → `928px 248px` (main + 248px rail); current pipeline node → blue border `rgba(10,132,255,0.55)` + blue icon, node box 138×92px (pixel-match to mockup).
  - **Live-vs-mockup verdict:** MATCHES on the FIRST implementation iteration (the CSS defense was planned in up-front from the prior v3 root-cause dig, so the holographic-title trap never triggered). Header, properties strip, real pipeline diagram with "This agent" highlight on the parser node, Behavior (Won't), honest Recent-Results empty state with "Open test results" action, Activity timeline, "All clear" line, and the quiet right rail all render as designed. Screenshots: `review-screenshots/agent-overview-INAPP-after.png` (parser, full page), `…-INAPP-viewport.png`, `…-INAPP-standalone.png` (workspace standalone fallback). Verified across parser (entry), triage (parallel stage), and workspace (standalone) to confirm the topology/position/transport logic generalizes.
  - **Honesty self-audit:** ZERO fabricated values. Every rendered value traces to a real field, a real derived value (transport/position from real topology), a reused real helper (health, attention), or an honest empty state. Fields without a real source (Accepts, Returns, prompt version when 0 snapshots) are omitted, not invented.
  - **Build:** one-shot `npm --prefix client run build` passed (`✓ built in 2.23s`, no errors). Did NOT start/stop/restart any server — app was already live on :5174 (HMR); build was one-shot only.
- 2026-05-30: **DOUBLE-HEADER UNIFIED (option A — promote to page level).** The Overview tab showed identity+status TWICE: once in the page top-strip (`AgentProfileDetailPage`'s `profile-page-topbar`: "X's Profile" back-link + "Agent status" enable toggle + Active/Inactive) and again in the new `.agent-overview-v2` `aov-header` (avatar + name + purpose + Enabled + Online). The top-strip was ALSO the only identity + only enable toggle on the other 11 tabs, so it could not just be deleted. Fix = consolidate into ONE page-level header.
  - **What moved where:** Added a new `UnifiedAgentHeader` component in `client/src/components/AgentsView.jsx` (placed just above `AgentProfileDetailPage`). It renders the promoted rich treatment at the page level — ABOVE the tab bar, so it shows on EVERY tab: left = back-link "‹ Agents" (breadcrumb to `#/agents`, folded in from the old top-strip) + avatar + name + purpose; right = the REAL enable/disable toggle (Enabled/Disabled + "Active in pipeline"/"Turned off"/"Saving…" caption) + a divider + the live Online/Offline health pill ("Checked Ns ago"). `AgentProfileDetailPage` no longer renders `profile-page-topbar`; it renders `<UnifiedAgentHeader>` instead, reading `selectedAgent`, `workspaceProps.selectedHealth`, `loadingCurrent`, `workspaceProps.enabledSaving`, `workspaceProps.onToggleAgentEnabled`. The old "Refreshing profile…" loader is folded in as a small `aph-refreshing` slot.
  - **Overview header REMOVED:** deleted the `aov-header` + `aov-rule` block from `AgentOverviewTab`; the Overview body now starts directly at the properties strip (Agent ID / Model / Tools) → pipeline. Cleaned up the now-unused header-only locals (`name`, `enabled`, `healthStatus`/`healthLabel`/`healthFresh`/`healthTone`) and dropped the `selectedHealth` prop from `AgentOverviewTab` (it's consumed by the page header now). Kept `purpose` (the Behavior section still uses it to decide whether the headline is distinct enough to show as "Does"). Removed the dead `.aov-header`/`.aov-head-*`/`.aov-monogram`/`.aov-rule`/`.aov-status-*`/`.aov-enabled-dot`/`.aov-health-dot` CSS and reduced `.agent-overview-v2` top padding (28px → 8px) now that it has no header.
  - **Enable toggle still wires to the real handler — confirmed:** `UnifiedAgentHeader`'s checkbox `onChange` → `onToggleAgentEnabled?.(checked)` = `workspaceProps.onToggleAgentEnabled` = `handleToggleAgentEnabled` (AgentsView.jsx ~865), which sets `enabledSaving`, opens the lifecycle modal, and calls the real `updateAgentEnabled(...)` PATCH /enabled flow (~899). Disabled while `enabledSaving`. Wiring byte-for-byte preserved — only the surrounding markup/styling changed. The pre-existing `profile-page-topbar` / `agent-enabled-switch` CSS in AgentsView.css is now unused on this route but was left intact (out of scope; harmless dead CSS).
  - **Global-CSS defense on the NEW page-level header:** the unified header is a `<header>` element, so it is back in the firing line of `overhaul.css` (`header [class*="title"]` transparent-fill, `header::before` blur). Scoped it under `.agent-profile-header` with self-contained tokens (it lives OUTSIDE `.agent-overview-v2`); NO class contains the substring "title"; added a header-scoped guard `.agent-profile-header, .agent-profile-header * { -webkit-text-fill-color: currentColor }`; pinned the name's own opaque fill (`.aph-name { -webkit-text-fill-color: var(--aph-primary) !important; background: none !important }`); and neutralized `header::before`/`::after` with `content: none !important`.
  - **Verified by computed style (live):** OVERVIEW tab → `.agent-profile-header` count = 1, `.aov-header`/`.aov-name` count = 0, total name elements on page = 1 (no duplicate). `.aph-name` → `color/webkitTextFillColor rgb(245,245,247)`, `backgroundImage none` (NOT transparency-bombed). `header::before` → `content none`. Toggle = real `<input type=checkbox checked>`. Health dot = `tone-online`. Header bottom (159px) ≤ tab bar top (169px) → header sits above tabs. Overview body first label = "Agent ID". CONFIGURATION tab → unified header present, name solid (`fill rgb(245,245,247)`, `bg none`), toggle checked, `.agent-overview-v2` count = 0 (Overview body correctly only on the Overview tab), total name elements = 1.
  - **Verdict:** the double-header is GONE — every tab now shows ONE premium header (identity + status + working enable toggle), zero redundancy; the Overview content is header-less and flows straight into its sections. The header looks good on both the Overview tab and the Configuration tab (verified visually + by computed style).
  - **Screenshots:** `review-screenshots/agent-profile-unified-header-overview.png` (Overview) and `…-othertab.png` (Configuration).
  - **Build:** one-shot `npm --prefix client run build` passed (`✓ built in 2.43s`, no errors). Did NOT start/stop/restart any server — app was already live on :5174 (HMR); build was one-shot only.
- 2026-05-30: **Properties strip polish — plain-mono technical values + provider logos.** Two user-requested tweaks to `AgentOverviewTab`'s properties strip (Agent ID / Model / Tools) in `client/src/components/AgentsView.jsx` + `AgentOverviewTab.css`.
  - **PART 1 — Agent ID + Model now plain monospace, NO box.** Root cause of the cramped/boxed look: `overhaul.css` line 361 `code, pre { background:#3a3a3c !important; color:#f5f5f7 !important; border:1px solid rgba(84,84,88,.35) !important; border-radius:8px !important }` boxes EVERY `<code>` element (the Agent ID and Model values render as `<code class="aov-mono">`), and its `!important` overrode the chrome-light intent. Fix = a scoped override `.agent-overview-v2 code.aov-mono { background:none !important; border:none !important; border-radius:0 !important; padding:0 !important; color:var(--aov-primary) !important }` (the `!important` is required to beat the global `!important`). They stay distinct via the monospace font alone. Verified live by computed style: both `escalation-template-parser` and `gemini-3.5-flash` → `backgroundColor rgba(0,0,0,0)`, `borderTopWidth 0px`, `borderRadius 0px`, `paddingLeft 0px`.
  - **PROVIDER LOGO ASSETS FOUND (verified real, non-empty files) in `client/public/provider-icons/`:** `anthropic.png` (1.4 KB), `gemini.svg` (27.8 KB), `kimi.ico` (17 KB), `lm-studio.webp` (13.6 KB), `openai.svg` (1.7 KB), `openai-dark.svg` (1.7 KB). The user's belief that "SVGs of all the provider logos exist" is PARTLY right: there ARE logo assets for all the real providers, but they are a MIX of formats (only OpenAI + Gemini are `.svg`; Anthropic is `.png`, Kimi is `.ico`, LM Studio is `.webp`) — not all SVG. The **canonical provider→asset mapping already exists** in `shared/ai-provider-catalog.json` via the `iconPath` (and `iconLightPath` for OpenAI/Codex) fields, surfaced through `getProviderMeta()` in `client/src/lib/providerCatalog.js`, and already rendered by the app's header provider picker (`ProviderLogo` in `client/src/components/app/AppHeader.jsx`). I REUSED that exact source rather than inventing a new map. Mapping: claude/claude-opus-4-8/anthropic → `/provider-icons/anthropic.png`; codex/openai/gpt-5.* → `/provider-icons/openai-dark.svg` (light: `openai.svg`); gemini → `/provider-icons/gemini.svg`; kimi → `/provider-icons/kimi.ico`; lm-studio → `/provider-icons/lm-studio.webp`.
  - **PART 2 — provider logo next to the Model value.** New `ProviderModelLogo` component (AgentsView.jsx) renders a ~16px inline icon picked by the agent's REAL runtime PROVIDER id (`normalizeAgentRuntimeState(def, agent.runtime).provider`, which matches the catalog id) → `getProviderMeta(providerId)` → `iconLightPath || iconPath` (light variant preferred on the dark surface, matching AppHeader). For the parser (provider `gemini`) it shows the real Gemini logo. Verified live: `imgSrc /provider-icons/gemini.svg`, loaded (`naturalWidth 150`, `complete true`), 16px, `title "Gemini"`, NOT the fallback. CSS: `.aov-provider-logo` (16px flex) + `.aov-provider-logo img` (object-fit contain, 3px radius).
  - **MISSING-LOGO HANDLING (no fabrication):** the ONLY catalog provider with NO icon asset is **`llm-gateway`** (it uses `"iconStrategy":"runtime-model-family"` with no static file). For it (and any future provider lacking an asset) `ProviderModelLogo` renders a tasteful neutral monogram chip built from the provider's REAL short label initial — never a fabricated logo. An `<img onError>` handler also degrades to the same monogram if a file ever fails to load. **Provider lacking a logo, to source later: `llm-gateway` (LLM Gateway API).**
  - **Honesty + CSS defense:** logo is a real catalog asset or an honest neutral fallback (no invented art). Still scoped under `.agent-overview-v2`, no class contains "title", the text-fill guard remains. The new `<img>` carries `alt=""`/`aria-hidden` (decorative; the provider is already named in the adjacent "Google Gemini API" sublabel).
  - **Verdict:** Agent ID + Model read as clean chrome-light monospace with no boxes; the real Gemini provider logo sits next to the model. Looks right at the chrome-light bar. Screenshots: `review-screenshots/agent-overview-mono-and-logo.png` (full Overview) and `…-props-closeup.png` (properties strip close-up clearly showing the Gemini mark + unboxed mono).
  - **Build:** one-shot `npm --prefix client run build` passed (`✓ built in 2.91s`, no errors). Did NOT start/stop/restart any server — app already live on :5174 (HMR); build one-shot only. (Note: during verification the app briefly showed its own "Server unreachable" boot overlay; `/api/health` on :4000 returned 200 and :5174 returned 200, so it was a transient boot blip — cleared on reload, no server action taken.)
- 2026-05-30: **Compactness + spacing-consistency pass** on the unified header + the whole Overview body (`UnifiedAgentHeader` in `client/src/components/AgentsView.jsx`, `AgentOverviewTab.css`). Spacing was treated as a first-class pass/fail this round.
  - **PRIORITY 1 — slim cross-tab header. Header height 109px → 65px** (measured live, ~40% shorter / about half the previous slack). Changes: avatar 52px → **36px** (`size="large"` → `size="small"`); name 24px/700 → **19px/600**; the breadcrumb was collapsed from its OWN stacked line into an inline **28px back-chevron button** at the far left of a single horizontal identity row (markup: `.aph-left` changed from `flex-direction:column` to `row`; the `.aph-identity` wrapper was removed so back + avatar + name/purpose sit on one row); purpose is now a single truncating line (`white-space:nowrap; overflow:hidden; text-overflow:ellipsis; max-width:520px`, with a `title` tooltip for the full text); vertical padding `8px/20px` → **`12px/12px`** per side; header gap 32 → 24, `.aph-right` gap 20 → 16, status-item gap 4 → 2. The avatar monogram radius/inner radius dropped to match the smaller chip (8/7px). Still elegant, not cramped (verified on the header close-up).
  - **PRIORITY 2 — one 8px scale (4 / 8 / 12 / 16 / 24 / 32) across header + body, no cramped/loose spots.** Notable values changed: body root padding-bottom 48 → **32**; body-grid gap 48 → **32**; **every `.aov-section` padding-top 40 → 24** (uniform inter-section rhythm — verified live: Pipeline/Behavior/Recent Results/Activity all read 24px); section-overline margin-bottom 20 → **12**; props strip padding `20px 0` → **16px 0**, `.aov-prop` gap 5 → **4**; result-stats gap 48 → **32**, result-stat gap 2 → **4**; rail gap 40 → **24**, rail padding-left 32 → **24**, rail-row gap 3 → **4**; activity `.aov-tl-item` padding-bottom 20 → **16**; all-clear margin-top 32 → **24** + padding-top 20 → **16**; standalone/empty stray `4px 0` paddings → **0** (the 24px section padding-top already owns the rhythm); standalone-sub / empty-sub margin-top 3 → **4**; empty-action padding `7px 12px` → **8px 12px**. Intentional sub-grid micro-values left as-is: timeline connector nudges (`margin: 2px 0 -2px` on `.aov-tl-line`, `padding-top:2px` on `.aov-tl-body`) and the 16px provider-logo, which are alignment tweaks, not rhythm spacing.
  - **Cleanup:** removed a now-dead `@media(max-width:760px) .aov-header` rule (the Overview no longer has its own `.aov-header` — that header was promoted to `.agent-profile-header` in the prior unify pass, which has its own 760px rule).
  - **Honesty + CSS defense intact:** no data changed; scope was the header + AgentOverviewTab + its CSS only; no other tab content touched, no shared components deleted. Still `.agent-overview-v2` / `.agent-profile-header` scoped, no class contains "title", the `-webkit-text-fill-color: currentColor` guard remains and the name pin holds — verified live the compact name renders solid (`webkitTextFillColor rgb(245,245,247)`, `backgroundImage none`).
  - **VERDICT — both criteria PASS:** (a) **Compactness PASS** — header is a slim 65px band (was 109px), avatar 36px, title 19px/600, breadcrumb+avatar+name+purpose on one tight row, status cluster compact and right-aligned; verified compact on BOTH the Overview tab and the Test Results tab (the band is identical cross-tab). (b) **Spacing consistency PASS** — every audited padding/margin/gap is on the 4/8/12/16/24/32 scale; inter-section rhythm is a uniform 24px; no cramped spots, no over-loose gaps (verified by live computed-style measurement, not just the screenshot).
  - **Screenshots:** `review-screenshots/agent-overview-compact-spacing.png` (Overview), `…-compact-spacing-full.png` (full-page Overview rhythm), `…-compact-spacing-othertab.png` (Test Results tab — same slim header), `…-header-closeup.png` (header detail).
  - **Build:** one-shot `npm --prefix client run build` passed (`✓ built in 2.57s`, no errors). Did NOT start/stop/restart any server — app already live on :5174 (HMR); build one-shot only.
- 2026-05-30: **PIPELINE DIAGRAM WENT TO PRODUCTION — animated dataflow (replaced the static `PipelineDiagram`).** The Overview's Pipeline section now renders the user-confirmed animated SVG dataflow ported faithfully (NOT redesigned) from `prototypes/agent-card/pipeline-animated.html`. The static stepped diagram (`.aov-pipeline` / `.aov-pl-*`) is gone; the animated version (`.aov-flow` / `.aov-flow-*` / `.aov-wire-*`) is in.
  - **Files changed:** `client/src/components/AgentsView.jsx` (`PipelineDiagram` rewritten — animated SVG wire layer + node grid + a `useEffect` controller for the staggered entrance, the gated-ignition loop, and the `prefers-reduced-motion` static fallback; timers cleaned up on unmount), `client/src/components/AgentOverviewTab.css` (the static `.aov-pipeline`/`.aov-pl-*` block replaced by the scoped animated `.agent-overview-v2 .aov-flow*` styles — role-color tokens, node states, wire keyframes, reduced-motion + responsive blocks; the dead `@media(760px) .aov-pipeline` line removed), `client/src/components/chat-v5/pipelineRuntime.js` (PIPELINE_TOPOLOGY correction — see below).
  - **PIPELINE_TOPOLOGY CORRECTION:** `chat` (the QBO Assistant) `.upstream` changed from `['known-issue-search-agent','triage-agent']` to `['escalation-template-parser','known-issue-search-agent','triage-agent']`. The Assistant's prompt is composed from the parser's structured fields PLUS INV PLUS triage (verified data flow in `server/src/routes/chat/send.js`), so the parser is a real direct upstream of the Assistant — reflected by the diagram's straight `parser→Assistant` "parser context (direct)" spine running alongside the INV/Triage merge. `parser` stays the entry (no upstream); INV + Triage stay parallel. `PIPELINE_TOPOLOGY_ORDER` / `pipelinePosition` were NOT changed (the parser still occupies sequential step 1; the new edge is a dependency edge, not a new ordering position), so "Step N of 3" stays honest.
  - **"This agent" highlight GENERALIZES (not hardcoded to the parser):** geometry/layout is FIXED (identical to the prototype) and each node keeps its permanent informational ROLE color (Parser=blue hero, INV=teal, Triage=violet, Assistant=indigo/gated). Only the ★ "This agent" pin + an accent `is-current` ring move to whichever pipeline agent's profile is being viewed, driven by `agentId === <nodeId>`. Verified live: viewing the parser → pin on Image Parser; viewing triage-agent → pin moved to the Triage node (Image Parser keeps blue, no pin). The branch nodes host the pin absolutely-positioned so the compact card body doesn't reflow.
  - **CRITICAL CROSS-BROWSER FIXES PRESERVED VERBATIM (headless cannot catch these — they only fail in the user's real GPU-composited Chrome):** (1) all three STRAIGHT-LINE flow gradients (`#aovG-entry`, `#aovG-pass`, `#aovG-combined`) use `gradientUnits="userSpaceOnUse"` with explicit x1/y1/x2/y2 (NOT objectBoundingBox, which is degenerate → invisible on a flat stroke); (2) NO `filter`/blur on ANY animated stroke (grep-confirmed: zero `filter` attributes in the SVG; the only "filter" tokens in the file are unrelated list-filter logic and comments documenting the absence); (3) the fork/branch flow pulses ORIGINATE at the Image Parser card's right edge (`x=544`), not at a fork point; (4) the 3 merge streams converge → a single combined pulse continues INTO the Assistant; (5) gated ignition ("Waiting · 3 inputs" → "Active"); (6) `prefers-reduced-motion` static fallback (no motion, Assistant shown already-ignited); (7) plain `.aov-wire-flow` `stroke-dashoffset` technique — no comet/head/filter machinery.
  - **Global-CSS (overhaul.css) defense:** still scoped under `.agent-overview-v2`; NO new class contains the substring "title"; the card-wide `-webkit-text-fill-color: currentColor` guard covers the new subtree. The global `svg { color: inherit !important }` only affects `color` — the wires are stroked via explicit `stroke`/gradient attributes (never `color`), so it cannot wash them; the universal `* { transition-timing-function … !important }` and `backdrop-filter: none !important` rules don't block `stroke-dashoffset` keyframe animation.
  - **Standalone fallback unchanged:** agents NOT in PIPELINE_TOPOLOGY (e.g. workspace, copilot) still render the honest "Standalone agent — Not part of the escalation pipeline" state (no diagram). Verified live on `workspace`.
  - **Build:** one-shot `npm --prefix client run build` passed (`✓ built in 2.69s`, no errors). Did NOT start/stop/restart any server — app already live on :5174 (HMR); build one-shot only.
  - **Screenshots:** `review-screenshots/overview-pipeline-parser.png` (parser — ★ pin on Image Parser), `…-triage.png` (triage-agent — pin moved to Triage node), `…-workspace-standalone.png` (workspace standalone fallback). NOTE: the screenshots confirm the diagram RENDERS correctly (layout, colors, node labels, highlight, no clashing) and is not broken — but the MOTION itself (the traveling pulses, breathing halo, sync-blink, gated ignite) must be confirmed in the USER'S REAL CHROME; headless capture is a single still frame and cannot validate the cross-browser animation behavior that the userSpaceOnUse / no-filter fixes exist to protect.

- 2026-05-30: **PIPELINE DIAGRAM ALIGNMENT BUG FIXED — wires now glued to node edges at every width (shared coordinate space).** User-reported on the live Overview: the wires did NOT line up with the node boxes (connectors offset/floating, uneven spacing). This was a port artifact — the SVG wire layer (`viewBox="0 0 1180 343"` + `preserveAspectRatio="xMidYMid meet"`) and the node grid (fixed-px column track) were two INDEPENDENT layout systems. The wire coords were tuned to the prototype's exact 1180×343 stage; in the app the Pipeline section renders only ~890px wide, so the SVG letterboxed and scaled by a DIFFERENT factor than the gutterless centered grid → drift.
  - **Measured BEFORE (live, at the panel's 890px width):** SVG letterboxed (drawn 890×259 inside an 890×321 box, scale 0.754, padY=31). Wire-endpoint vs node-edge offsets were large and NON-uniform: operator-right **+73px**, parser-left **+59px**, INV-right **−54px**, assistant-left **−69px**. Even forcing the wrapper to the design 1180px did NOT fix it (still padX=39 letterbox, offsets up to ±18px) — confirming two coordinate systems that never share a mapping. The standalone prototype, by contrast, measured 0px offsets everywhere (it renders at exactly 1180×343, scale 0.9986, padX≈1).
  - **FIX (shared coordinate space, width-independent by construction):** both the wire SVG and the node grid now live inside a single new `.aov-flow-stage` div sized at the FIXED design dimensions **1180×343px** — exactly the layout the wire coords were tuned against. The fluid wrapper `.aov-flow` scales that whole stage as ONE unit via `transform: scale(var(--aov-scale))` (top-left origin), with `--aov-scale` driven by a `ResizeObserver` on the wrapper (`min(1, availableWidth/1180)`). Because the entire subtree (wires + nodes) scales by the same factor with NO reflow, the wires stay glued to the node edges at every width. The wrapper reserves the scaled height via `aspect-ratio: 1180/343`, so the SVG renders 1:1 with its viewBox (zero letterbox: padX=padY=0). The grid fills the stage (`height:100%`); `justify-content:center` reproduces the prototype's 124px side gutters (932px of columns centered in 1180). Branch (INV/Triage) node `min-height` was pinned **78px → 117px**: the fork/merge curve endpoints assume the two branch node centers are 173 design units apart (y=85 / y=258), and center-separation = nodeHeight(117) + gap(56) = 173; the app's branch nodes have fewer text lines than the prototype so they were collapsing to ~106px and the centers drifted ~11px off the curves. Files: `client/src/components/AgentsView.jsx` (`PipelineDiagram` — new `stageRef` + ResizeObserver effect + `.aov-flow-stage` wrapper around the SVG/grid), `client/src/components/AgentOverviewTab.css` (`.aov-flow` wrapper, new `.aov-flow-stage`, grid `height:100%`, branch `min-height`, responsive `<=1100px` resets aspect-ratio/transform).
  - **Measured AFTER (live):** at the panel's 890px width EVERY offset is **0px** (operator-right, parser-left, parser-right, INV-right, Triage-right, assistant-left, INV-centerY, Triage-centerY, spine-midY) — the lone residual is the intentional ~1px gap where the combined merge→assistant segment ends just short of the node edge (stable, by-design). **Robustness proven at multiple widths:** forced the wrapper to 1180 / 1000 / 760 / 620 / 600px (effective scales 1.0 → 0.51) — and at the 620px case the component's OWN ResizeObserver recomputed `--aov-scale=0.5254` — every offset stayed 0 (assistant-left 1) at all widths. This confirms it is NOT re-tuned for one width.
  - **NO regression to the hard-won cross-browser fixes:** no path coordinates were moved, so the `userSpaceOnUse` straight-line gradients (`#aovG-entry`, `#aovG-pass`, `#aovG-combined`) still match their path endpoints exactly (no gradient-coord update needed). Grep-confirmed: zero `filter` attributes on any animated stroke; fork pulses still originate at x=544; merge→combined pulse, gated ignition, reduced-motion fallback, and the plain `.aov-wire-flow` technique are all untouched. Live DOM check confirmed 5 nodes shown, gated ignition toggling (assistant `.ignited`), flow animations bound (`aov-flow` / `aov-flow-combined`), and no `filter` attr present.
  - **Build:** one-shot `npm --prefix client run build` passed (`✓ built in 2.36s`, no errors). Did NOT start/stop/restart any server — app already live on :5174 (HMR); build one-shot only.
  - **Screenshots:** `review-screenshots/pipeline-aligned-parser.png` (parser flow, wires landing on node edges), `…-pipeline-aligned-parser-full.png` (full stage incl. QBO Assistant + merge), `…-pipeline-aligned-narrow.png` (the SAME diagram at the constrained 620px width — uniformly smaller, wires still glued, proving width-independence). ALIGNMENT is fully verified here via getBoundingClientRect at multiple widths; the MOTION still needs the user's real-Chrome check (a still frame can't validate the pulse animation).
